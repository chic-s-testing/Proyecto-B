<p id="notice"><%= notice %></p>

<div class="container">
  <div class="section is-flex-shrink-2">
    <h1 class="title">Cartelera</h1>
    <%= link_to '+ Registrar Película', new_movie_path, class: "button is-primary is-medium"%>
  </div>
  <div class="container">
  <% @movies.each do |movie| %>
    <div class="box">
      <article  class="media">
        <div class="media-left">
          <figure class="image is-128x128">
          <img src=<%= movie.photo %> alt=<%= movie.name %>>
          </figure>
        </div>
      <div class="media-content">
        <div class="content">
          <p class="title is-4"><%= movie.name %></p>
        </div>
      </div>
    </article>
  </div>
  <% end %>
  </div>
  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Photo</th>
        <th>Initialdate</th>
        <th>Finaldate</th>
        <th colspan="3"></th>
      </tr>
    </thead>

    <tbody>
      <% @movies.each do |movie| %>
        <tr>
          <td><%= movie.name %></td>
          <td><%= movie.photo %></td>
          <td><%= movie.initialDate %></td>
          <td><%= movie.finalDate %></td>
          <td><%= link_to 'Show', movie %></td>
          <td><%= link_to 'Edit', edit_movie_path(movie) %></td>
          <td><%= link_to 'Destroy', movie, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
