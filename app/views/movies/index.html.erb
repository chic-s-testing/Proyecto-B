<p id="notice"><%= notice %></p>

<div class="container">
  <div class="section is-flex-shrink-2">
    <h1 class="title">Cartelera</h1>
    <%= link_to '+ Registrar Película', new_movie_path, class: "button is-primary is-medium"%>
  </div>
  <div class="container">
  <% @movies.each do |movie| %>
    <div class="box">
      <article  class="media">
        <div class="media-left">
          <figure class="image is-128x128">
          <img src=<%= movie.photo %> alt=<%= movie.name %>>
          </figure>
        </div>
      <div class="media-content">
        <div class="content">
          <p class="title is-4"><%= movie.name %></p>
          <p> Matine:  
          <span class="tag is-info is-light">SALA 1</span>
          </p>
          <p> Tanda:  
          <span class="tag is-info is-light">SALA 1</span>
          </p>
          <p> Noche:  
          <span class="tag is-info is-light">SALA 1</span>
          </p>
          <p>Funciones: <%= movie.initialDate %> hasta <%= movie.finalDate %></p>
          <%= link_to 'Ver pelicula', movie, class: "button is-primary is-small" %>
          <%= link_to 'Editar pelicula', edit_movie_path(movie), class: "button is-primary is-small" %>
          <%= link_to 'Eliminar pelicula', movie, method: :delete, data: { confirm: 'Are you sure?' }, class: "button is-primary is-small" %>

        </div>

      </div>
    </article>
  </div>
  <% end %>
  </div>
</div>
